'use client'

import SignupForm from "../components/signupForm";

export default function Signup() {
    return (
        <div>
            <SignupForm onSubmit={async () => {}} />
        </div>
    ); 
}